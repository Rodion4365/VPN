# OpenVPN Server для Selectel

Оптимизированная установка OpenVPN сервера с минимальной нагрузкой на сервер, идеально подходит для работы вместе с другими проектами (back, front).

## 📋 Содержание

- [Возможности](#возможности)
- [Быстрый старт](#быстрый-старт)
- [Подробная установка](#подробная-установка)
- [Управление клиентами](#управление-клиентами)
- [Мониторинг](#мониторинг)
- [Оптимизация ресурсов](#оптимизация-ресурсов)
- [Устранение неполадок](#устранение-неполадок)

## ✨ Возможности

### 🚀 Оптимизация производительности
- **Минимальная нагрузка на CPU**: Приоритет процесса снижен (nice 10)
- **Ограничение памяти**: Максимум 256 MB RAM через systemd
- **Ограничение CPU**: До 25% одного ядра
- **Эффективное шифрование**: AES-128-GCM для баланса безопасности и производительности
- **Оптимизированные буферы**: Настроены для минимальной задержки

### 🔒 Безопасность
- Современное шифрование (AES-128-GCM + SHA256)
- TLS-Auth для дополнительной защиты
- Поддержка отзыва сертификатов (CRL)
- Минимальная версия TLS 1.2

### 🛠 Управление
- Автоматическая установка одним скриптом
- Простое управление клиентами
- Мониторинг в реальном времени
- Автоматическая настройка firewall

## 🚀 Быстрый старт

### 1. Клонировать репозиторий на сервер

```bash
cd /opt
git clone <your-repo-url> openvpn-setup
cd openvpn-setup
```

### 2. Запустить установку

```bash
chmod +x install-openvpn.sh
sudo ./install-openvpn.sh
```

### 3. Создать клиента

```bash
chmod +x manage-clients.sh
sudo ./manage-clients.sh add laptop
```

### 4. Скачать конфигурацию

```bash
# На вашем локальном компьютере
scp root@YOUR_SERVER_IP:~/openvpn-clients/laptop.ovpn .
```

### 5. Подключиться

Импортируйте файл `laptop.ovpn` в ваш OpenVPN клиент и подключитесь!

## 📦 Подробная установка

### Требования

- Ubuntu 18.04+ / Debian 9+ / CentOS 7+
- Root доступ
- Минимум 512 MB RAM (256 MB для OpenVPN + 256 MB для системы)
- Открытый порт 1194/UDP в firewall Selectel

### Шаг 1: Подготовка сервера

```bash
# Обновить систему
sudo apt update && sudo apt upgrade -y

# Установить git если не установлен
sudo apt install -y git
```

### Шаг 2: Загрузить скрипты

```bash
cd /opt
git clone <your-repo-url> openvpn-setup
cd openvpn-setup
```

### Шаг 3: Настроить параметры (опционально)

По умолчанию используются следующие параметры:
- Порт: 1194
- Протокол: UDP
- Сеть VPN: 10.8.0.0/24

Для изменения экспортируйте переменные перед установкой:

```bash
export VPN_PORT=1194
export VPN_PROTOCOL=udp
```

### Шаг 4: Запустить установку

```bash
chmod +x install-openvpn.sh
sudo ./install-openvpn.sh
```

Скрипт автоматически:
- Установит необходимые пакеты
- Создаст сертификаты
- Настроит OpenVPN сервер
- Настроит firewall и маршрутизацию
- Запустит службу

### Шаг 5: Проверить статус

```bash
sudo systemctl status openvpn@server
```

### Шаг 6: Настроить firewall Selectel

В панели управления Selectel откройте порт 1194/UDP для вашего сервера.

## 👥 Управление клиентами

### Добавить клиента

```bash
sudo ./manage-clients.sh add <имя-клиента>
```

Примеры:
```bash
sudo ./manage-clients.sh add laptop
sudo ./manage-clients.sh add phone-android
sudo ./manage-clients.sh add work-macbook
```

После создания конфигурационный файл будет сохранен в `~/openvpn-clients/<имя-клиента>.ovpn`

### Скачать конфигурацию клиента

С вашего локального компьютера:

```bash
scp root@YOUR_SERVER_IP:~/openvpn-clients/<имя-клиента>.ovpn .
```

### Список всех клиентов

```bash
sudo ./manage-clients.sh list
```

### Удалить клиента

```bash
sudo ./manage-clients.sh remove <имя-клиента>
```

Это отзовет сертификат клиента и заблокирует доступ.

### Показать подключенных клиентов

```bash
sudo ./manage-clients.sh connected
```

## 📊 Мониторинг

### Запустить монитор в реальном времени

```bash
chmod +x monitor-vpn.sh
sudo ./monitor-vpn.sh
```

Монитор показывает:
- Статус службы и время работы
- Использование CPU и памяти
- Сетевой трафик
- Список подключенных клиентов
- Общую загрузку системы

### Быстрая проверка

```bash
sudo ./monitor-vpn.sh --quick
```

### Настройка интервала обновления

```bash
sudo ./monitor-vpn.sh --interval 5  # Обновлять каждые 5 секунд
```

### Просмотр логов

```bash
# Логи OpenVPN
sudo tail -f /var/log/openvpn/openvpn.log

# Статус подключений
sudo cat /var/log/openvpn/openvpn-status.log

# Системные логи службы
sudo journalctl -u openvpn@server -f
```

## ⚙️ Оптимизация ресурсов

### Встроенная оптимизация

OpenVPN настроен для минимальной нагрузки:

#### CPU оптимизация
- **Nice level 10**: Сниженный приоритет процесса
- **CPU Quota 25%**: Ограничение использования CPU
- **AES-128-GCM**: Быстрое шифрование с аппаратным ускорением

#### Оптимизация памяти
- **MemoryMax 256M**: Жесткое ограничение памяти
- **MemoryHigh 192M**: Мягкое ограничение (тротлинг)
- **Max clients 10**: Ограничение количества клиентов

#### Сетевая оптимизация
- **Оптимизированные буферы**: 393216 байт для send/receive
- **Compression off**: Экономия CPU
- **Efficient MTU**: Минимальная фрагментация

### Дополнительная оптимизация

#### Применить ограничения systemd

Для более строгих ограничений ресурсов:

```bash
# Скопировать оптимизированный unit файл
sudo cp openvpn-server.service /etc/systemd/system/openvpn-server@.service

# Перезагрузить конфигурацию
sudo systemctl daemon-reload

# Перезапустить службу
sudo systemctl restart openvpn@server
```

#### Настроить ограничения

Отредактируйте `/etc/systemd/system/openvpn-server@.service`:

```ini
# Ограничить CPU до 15%
CPUQuota=15%

# Уменьшить лимит памяти
MemoryMax=128M
MemoryHigh=96M

# Снизить приоритет I/O
IOWeight=25
```

После изменений:

```bash
sudo systemctl daemon-reload
sudo systemctl restart openvpn@server
```

### Мониторинг влияния на другие проекты

```bash
# Проверить использование ресурсов всеми процессами
top

# Проверить конкретные службы
systemctl status openvpn@server
systemctl status your-backend-service
systemctl status your-frontend-service

# Использовать наш монитор
sudo ./monitor-vpn.sh
```

## 🔧 Устранение неполадок

### OpenVPN не запускается

```bash
# Проверить статус
sudo systemctl status openvpn@server

# Проверить логи
sudo journalctl -xe -u openvpn@server

# Проверить конфигурацию
sudo openvpn --config /etc/openvpn/server.conf
```

### Клиент не может подключиться

1. **Проверить firewall на сервере:**
```bash
sudo iptables -L -n -v | grep 1194
```

2. **Проверить firewall Selectel:**
   - Убедитесь что порт 1194/UDP открыт в панели управления

3. **Проверить IP forwarding:**
```bash
sudo sysctl net.ipv4.ip_forward
# Должно быть: net.ipv4.ip_forward = 1
```

4. **Проверить логи:**
```bash
sudo tail -f /var/log/openvpn/openvpn.log
```

### Медленное подключение

1. **Проверить загрузку сервера:**
```bash
sudo ./monitor-vpn.sh --quick
```

2. **Проверить MTU:**
```bash
# На клиенте добавить в .ovpn:
mssfix 1400
tun-mtu 1500
```

3. **Переключиться на TCP (медленнее, но стабильнее):**
```bash
# В /etc/openvpn/server.conf:
proto tcp
port 1194
```

### Высокое использование ресурсов

1. **Уменьшить количество клиентов:**
```bash
# В /etc/openvpn/server.conf:
max-clients 5
```

2. **Применить строгие лимиты systemd:**
```bash
sudo cp openvpn-server.service /etc/systemd/system/openvpn-server@.service
sudo systemctl daemon-reload
sudo systemctl restart openvpn@server
```

3. **Проверить другие процессы:**
```bash
top
htop  # если установлен
```

## 📁 Структура файлов

```
/etc/openvpn/
├── server.conf           # Конфигурация сервера
├── ca.crt               # Сертификат CA
├── server.crt           # Сертификат сервера
├── server.key           # Ключ сервера
├── dh.pem               # Diffie-Hellman параметры
├── ta.key               # TLS-Auth ключ
├── crl.pem              # Список отозванных сертификатов
└── easy-rsa/            # PKI и управление сертификатами

/var/log/openvpn/
├── openvpn.log          # Основные логи
└── openvpn-status.log   # Статус подключений

~/openvpn-clients/       # Конфигурации клиентов
└── *.ovpn
```

## 🔄 Обновление и обслуживание

### Обновить пакеты

```bash
sudo apt update
sudo apt upgrade openvpn
sudo systemctl restart openvpn@server
```

### Резервное копирование

```bash
# Бэкап сертификатов и конфигураций
sudo tar -czf openvpn-backup-$(date +%Y%m%d).tar.gz \
    /etc/openvpn/ca.crt \
    /etc/openvpn/server.crt \
    /etc/openvpn/server.key \
    /etc/openvpn/ta.key \
    /etc/openvpn/dh.pem \
    /etc/openvpn/server.conf \
    /etc/openvpn/easy-rsa/pki
```

### Восстановление

```bash
# Распаковать бэкап
sudo tar -xzf openvpn-backup-YYYYMMDD.tar.gz -C /

# Перезапустить службу
sudo systemctl restart openvpn@server
```

## 🔗 Полезные ссылки

- [Официальная документация OpenVPN](https://openvpn.net/community-resources/)
- [Документация Selectel](https://docs.selectel.ru/)
- [Безопасность OpenVPN](https://community.openvpn.net/openvpn/wiki/Hardening)

## 📝 Лицензия

MIT License - используйте свободно для личных и коммерческих проектов.

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте раздел [Устранение неполадок](#устранение-неполадок)
2. Просмотрите логи: `sudo tail -f /var/log/openvpn/openvpn.log`
3. Используйте монитор: `sudo ./monitor-vpn.sh`

---

**Примечание**: Эта установка оптимизирована для работы вместе с другими проектами на том же сервере. OpenVPN настроен с низким приоритетом и строгими ограничениями ресурсов, чтобы не мешать вашим back и front проектам.